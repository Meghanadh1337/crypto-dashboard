// src/components/Header.js
import React from 'react';
import './Header.css'; // Separate CSS file for the header styling

const Header = () => {
    return (
        <header className="header">
            <h1>Coincheck</h1>
        </header>
    );
};

export default Header;
